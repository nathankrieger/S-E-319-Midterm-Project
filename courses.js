let row = `<div class="row g-4 py-5 row-cols-1 row-cols-lg-3"></div>`
let column = `<div class="col d-flex align-items-start">
<div>
  <h3 class="fs-2 text-body-emphasis">${courseTitle}</h3>
  <p>${courseDesc}</p>
  <a href="#" class="btn btn-primary">
    Go to course
  </a>
</div>
</div>`

function generateClasses() {
    
}
